# Frontend — folgt in Phase 5

Bewusst noch nicht aufgesetzt, um veraltendes Boilerplate zu vermeiden. Geplant:

- **React + Vite** (SPA), TypeScript.
- Ansichten: Themen/Wiki, Suche (hybrid), Sitzungsansicht, Matching-Inbox, Tags, Admin
  (siehe [../docs/KONZEPT.md](../docs/KONZEPT.md#7-frontend-ansichten)).
- Auslieferung als statischer Build über Caddy (siehe [../infra/Caddyfile](../infra/Caddyfile)).

Bis dahin liefert das Backend die API unter `/api` (siehe [../backend](../backend)).
